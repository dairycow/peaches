---
description: Initialise a worktree and execute the plan in it
agent: build
---

$1

Start by creating a new git worktree by running: `./create-worktree.sh <worktree-name>`.

Before starting execution:
1. Confirm the worktree was created successfully
2. Change to new worktree directory
3. Prefix all commands to use the new worktree directory.
4. Begin executing the plan

DO NOT STOP UNTIL THE PLAN IS COMPLETELY EXECUTED.
